services:
  backend:
    build:
      context: ./backend
    container_name: raidware-backend
    ports:
      - "6667:5000"
    restart: always
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-http://5.189.167.55:6667}
    container_name: raidware-frontend
    ports:
      - "6666:3000"
    restart: always
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
