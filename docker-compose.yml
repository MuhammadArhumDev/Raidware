services:
  backend:
    build:
      context: ./backend
    container_name: raidware-backend
    ports:
      - "3334:5000"
    restart: always
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-http://5.189.167.55:3334}
    container_name: raidware-frontend
    ports:
      - "3333:3000"
    restart: always
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
